spring:
  application:
    name: click_friends
  datasource:
    url: jdbc:mysql://click-mysql-friend:3306/click_friends?&serverTimezone=Asia/Seoul
    username: ${username}
    password: ${password}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

token:
  secret: ${secret}
